services:
  groupdocs-metadata-mcp:
    image: ghcr.io/groupdocs-metadata/metadata-net-mcp:26.7.1
    stdin_open: true
    tty: false
    environment:
      GROUPDOCS_MCP_STORAGE_PATH: /data
    volumes:
      - ./documents:/data
